{
  "name": "rag-slides-demo",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --remote",
    "deploy": "wrangler deploy",
    "test": "node --experimental-strip-types --test test_app.ts && tsc"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260901.1",
    "typescript": "^5.9.2",
    "wrangler": "^4.33.1"
  },
  "dependencies": {
    "fflate": "^0.8.3"
  }
}
